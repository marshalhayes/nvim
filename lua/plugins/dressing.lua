-- The dressing plugin is a plugin that makes inputting text easier
-- For example, when renaming variables, this plugin variable popups up a nice window
return {
	"stevearc/dressing.nvim",
	opts = {},
}
